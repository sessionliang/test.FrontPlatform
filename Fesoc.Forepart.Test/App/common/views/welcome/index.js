(function () {
    appModule.controller('common.views.welcome.index', [
        '$scope',
        function ($scope) {
            var vm = this;

            //注册页面加载事件
            $scope.$on('$viewContentLoaded', function () {
                
            });
        }
    ]);
})();