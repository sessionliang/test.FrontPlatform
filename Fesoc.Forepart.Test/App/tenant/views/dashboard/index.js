(function () {
    appModule.controller('tenant.views.dashboard.index', [
        '$scope',
        function ($scope) {
            var vm = this;

            $scope.$on('$viewContentLoaded', function () {

            });
        }]);
})();